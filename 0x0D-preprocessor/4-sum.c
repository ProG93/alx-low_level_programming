#include <stdio.h>
#include "4-sum.h"

/**
 * main - computes the sum of numbers
 * Return: 0
 */

int main(void)
{
	int x = 50;
	int y = 20;

	prinf("The sum is %d.\n", x, y, sum(x, y));
	return (0);
}
